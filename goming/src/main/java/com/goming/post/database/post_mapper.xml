<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goming.post.database.post_mapper">
	<!-- 포스트 게시 -->
	<insert id="postInsert" parameterType="com.goming.post.model.PostDTO">
		INSERT INTO post_info (user_email, post_img, gender, style_tag, season, comment, created_at)
		VALUES (#{user_email}, #{post_img}, #{gender}, #{style_tag}, #{season}, #{comment}, now());
	</insert>
	
	<!-- 포스트 필터링 -->
    <select id="getPostsByFilters" parameterType="com.goming.post.model.PostDTO" resultType="com.goming.post.model.PostDTO">
        SELECT post_idx, post_img, comment
        FROM post_info
        WHERE gender = #{gender}
          AND style_tag = #{style_tag}
          AND season = #{season}
    </select>   


	<select id="getidx" resultType="String">
		SELECT post_idx
		  FROM post_info
		 ORDER BY post_idx desc
		 LIMIT 1
	</select>

	<select id="getimg" resultType="String">
		SELECT post_img
		  FROM post_info
		 ORDER BY post_idx desc
		 LIMIT 1
	</select>


	<update id="updateImg" parameterType="com.goming.post.model.PostDTO">
		UPDATE post_info 
		SET post_img = #{postImg} 
		WHERE post_idx = #{postIdx}
	</update>

</mapper>