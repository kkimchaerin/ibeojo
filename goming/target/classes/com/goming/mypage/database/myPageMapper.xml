<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="myPageMapper">

<select id="selectOne" parameterType="com.goming.mypage.model.MyPageDTO" resultType="com.goming.mypage.model.MyPageDTO">
	select *
	  from user_info
	 where user_email = #{user_email} and user_pw = #{user_pw}
</select>

<select id="img_select" parameterType="com.goming.mypage.model.MyPageDTO" resultType="String">
	select post_img
	  from post_info
	 where user_email = #{user_email}
</select>

<delete id="delete_user" parameterType="com.goming.mypage.model.MyPageDTO">
	delete from user_info where user_email = #{user_email} and user_nick = #{user_nick} and user_pw = #{user_pw} 
</delete>


</mapper>
