<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goming.post.database.post_mapper">
	<insert id="postInsert" parameterType="com.goming.post.model.PostDTO">
		INSERT INTO post_info (user_email, post_img, gender, style_tag, season, created_at)
		VALUES (#{userEmail}, #{postImg}, #{gender}, #{style}, #{season}, now());
	</insert>
	
	<!-- 필터에 따라 포스트를 검색하는 쿼리 정의 -->
    <select id="getPostsByFilters" parameterType="com.goming.post.model.PostDTO" resultType="java.lang.String">
        SELECT post_img
        FROM post_info
        WHERE gender = #{gender}
          AND style_tag = #{style}
          AND season = #{season}
    </select>
	

	<select id="getidx" resultType="String">
		SELECT post_idx
		  FROM post_info
		 ORDER BY post_idx desc
		 LIMIT 1
	</select>

	<select id="getimg" resultType="String">
		SELECT post_img
		  FROM post_info
		 ORDER BY post_idx desc
		 LIMIT 1
	</select>


	<update id="updateImg" parameterType="com.goming.post.model.PostDTO">
		UPDATE post_info 
		SET post_img = #{postImg} 
		WHERE post_idx = #{postIdx}
	</update>

	<delete id="deleteImg" parameterType="String">
		DELETE FROM post_info WHERE post_img = #{post_img}
	</delete>

</mapper>